import assert from "node:assert/strict"
import fs from "node:fs"
import test from "node:test"
const read=(path)=>fs.readFileSync(new URL(`../${path}`,import.meta.url),"utf8")
test("Privacy Centre grant API requires an explicit history mode for a new Journey response grant",()=>{const route=read("app/api/privacy/sharing-grants/route.ts");assert.match(route,/addingJourneyResponses/);assert.match(route,/parseJourneyResponseHistoryMode/);assert.match(route,/include_pre_grant_data/);assert.match(route,/professional_journey_response_sharing/);assert.match(route,/action: "revoked"/)})
test("professional invitation acceptance cannot silently choose Journey response history",()=>{const route=read("app/api/connect/professional/route.ts");assert.match(route,/wantsJourneyResponses/);assert.match(route,/journeyResponsesHistoryMode/);assert.match(route,/include_pre_grant_data/);assert.match(route,/professional_journey_response_sharing/);assert.match(route,/journeyResponseCount/)})
test("professional response reads enforce the active grant timestamp server-side",()=>{const route=read("app/api/professional/clients/[linkId]/summary/route.ts");assert.match(route,/data_scope='journey_responses'|data_scope = 'journey_responses'/);assert.match(route,/last_completed_at >= \$\{grant\.granted_at\}/);assert.match(route,/professionalCanAccessClientData/);assert.match(route,/l\.status='active'|l\.status = 'active'/);assert.match(route,/journeyResponseCount/);assert.doesNotMatch(route,/metadata:\s*\{[^}]*response_data/s)})
test("privacy overview and export expose user-owned current responses without changing professional permission",()=>{const overview=read("app/api/privacy/overview/route.ts");const exportRoute=read("app/api/privacy/export/route.ts");assert.match(overview,/journeyResponseCount/);assert.match(overview,/includePreGrantData/);assert.match(exportRoute,/journey_module_responses/);assert.match(exportRoute,/journeyResponses/);assert.match(exportRoute,/formatVersion:"1\.2"|formatVersion: "1\.2"/)})
